# Where the API runs. For a phone on the same Wi-Fi use your PC's LAN IP, e.g. http://192.168.0.10:3000
EXPO_PUBLIC_API_URL=http://localhost:3000
# Must match APP_KEY on the API. Leave empty in local dev.
EXPO_PUBLIC_APP_KEY=
